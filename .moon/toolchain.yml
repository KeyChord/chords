$schema: "https://moonrepo.dev/schemas/toolchain.json"

# moon only orchestrates here; pnpm/node are managed as they already are.
node:
  packageManager: pnpm
  inferTasksFromScripts: false
  syncProjectWorkspaceDependencies: false
