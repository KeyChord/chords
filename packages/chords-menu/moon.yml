$schema: "https://moonrepo.dev/schemas/project.json"

layer: library
language: typescript

tasks:
  # `buildNative` in @keychord/config always builds; moon decides *whether* to,
  # by hashing the SwiftPM package and tracking target/ as the output.
  build-native:
    command: "vp pack"
    inputs:
      - "Sources/**/*"
      - "Package.swift"
      - "package.json"
      - "vite.config.ts"
    outputs:
      - "target"
