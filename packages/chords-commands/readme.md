# @keychord/chords-commands

Chord package for universal cross-platform commands.

Useful for making "universal shortcuts" such as Copy/Cut/Paste work cross-platform (ctrl on Windows, cmd on macOS).

## Prefix

Uses the `,` key as a prefix (why? because **Comma**nd)
